{
  "name": "@palmux/cli",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "__probe3": "node -e \"console.log(JSON.stringify({cwd:process.cwd(),INIT_CWD:process.env.INIT_CWD,PWD:process.env.PWD}))\""
  },
  "dependencies": {
    "@palmux/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
